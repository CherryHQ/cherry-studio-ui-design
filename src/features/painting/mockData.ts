// ===========================
// Image Generation — Mock Data
// ===========================
// Canonical location for painting module mock data.

export type {
  ImageModel, ImageMode, AspectRatio, ImageSize,
  GenerationParams, GeneratedImage,
} from '@/app/types/image';

export {
  IMAGE_MODELS,
  RATIO_DIMENSIONS,
  SIZE_LABELS,
  MOCK_IMAGES,
} from '@/app/mock/imageData';
